<%= form_with(url: search_results_path, method: "get", local: true) do |form| %>
  <div class="field">
    <%= form.text_field :query, id: "book_review_query", placeholder: "Search by Title or Author" %>
  </div>
  <%= form.submit "Search", class: "btn btn-primary" %>
<% end %>