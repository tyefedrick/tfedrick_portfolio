<%= form_for @genre, url: genre_path(@genre), method: :patch do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit "Update Genre", class: 'btn btn-primary' %>
<% end %>