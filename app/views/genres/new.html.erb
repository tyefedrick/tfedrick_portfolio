<%= form_for @genre do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit "Create Genre" %>
<% end %>

<!-- Display existing genres -->
<% if @existing_genres.any? %>
  <h2>Existing Genres:</h2>
  <ul>
    <% @existing_genres.each do |existing_genre| %>
      <li>
        <%= existing_genre.name %>
        <%= link_to "Show Options", genre_path(existing_genre) %>
      </li>
    <% end %>
  </ul>
<% end %>